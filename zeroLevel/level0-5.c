#include <stdio.h>

int main(){
    // looping for condition 5 biggers than 1
    for(int countDown = 5; countDown > 0; countDown--){
        // print number from looping
        printf("%d\n",countDown);
    }
    // last message shutdown
    printf("Sistem Shutdown\n");
}